#include <bits/stdc++.h>
using namespace std;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);

    /*
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);
    */

   /*
   #include "debugging.h"
   puede usarse para imprimir data structures
   
   */

    /*
    assertions
    #include <cassert>
    assert(false)

    #define NDEBUG
    para deshabilitarlas
    */



    return 0;
}